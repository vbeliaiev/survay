.content.attempts
  h3.pull-left= @quiz.title
  br
  br
  br
  = bootstrap_form_for [@quiz, @attempt] do |f|
    - @questions.each do |question|
      br
      = render partial: question.type, locals: { question: question }
      hr
    = f.submit class: 'btn btn-success pull-left', value: 'Подтвердить'
    = link_to 'Отмена', quizzes_path,  class: 'btn btn-danger a-cancel pull-left'
